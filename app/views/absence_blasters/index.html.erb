<nav class="top-bar" data-topbar role="navigation">
  <ul class="title-area">
    <li class="name">
      <h1><a href="#">Absence Blasters</a></h1>
    </li>
     <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <!-- Left Nav Section -->
  </section>
</nav>

<p id="notice"><%= notice %></p>

<table align="center">
  <thead>
    <tr>
      <th>Recipient name</th>
      <th>Recipient number</th>
      <th>Shift</th>
      <th>Access code</th>
      <th>Absence</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @absence_blasters.each do |absence_blaster| %>
      <tr>
        <td><%= absence_blaster.recipient_name %></td>
        <td><%= absence_blaster.recipient_number %></td>
        <td><%= absence_blaster.shift %></td>
        <td><%= absence_blaster.access_code %></td>
        <td><%= absence_blaster.absense %></td>
        <td><%= link_to 'Show', absence_blaster %></td>
        <td><%= link_to 'Edit', edit_absence_blaster_path(absence_blaster) %></td>
        <td><%= link_to 'Destroy', absence_blaster, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
